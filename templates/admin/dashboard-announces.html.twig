{# Entête de la page / Inclusion #}
{% include 'header.html.twig' %}
<body class="bg-light">

    {# Barre de Navigation #}
    {% include 'navigation.html.twig' %}

    {# Contenu de la page #}
    <div id="content" class="container mt-5 mb-5">
        <div class="container d-flex justify-content-between">
            <a href="/accueil" class="btn btn-danger mb-2">Retour</a>
            <a href="/panel/creeruneannonce" class="btn btn-success mb-2">Créer une annonce</a>
            <a href="/panel/utilisateurs" class="btn btn-info mb-2">Voir les Utilisateurs</a>
        </div>
        <div class="text-center mb-4">
            <h4>Administration des annonces</h4>
        </div>
        {% block dashboard_announces %}
            <table class="table mt-3 mb-3 text-center">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Titre</th>
                        <th>Description</th>
                        <th>Type</th>
                        <th>Surface</th>
                        <th>Prix</th>
                        <th>Image</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for annonce in announces %}
                        <tr>
                            <td>{{ annonce.id }}</td>
                            <td>{{ annonce.title }}</td>
                            <td>{{ annonce.description }}</td>
                            <td>{{ annonce.type }}</td>
                            <td>{{ annonce.surface }}</td>
                            <td>{{ annonce.price }}</td>
                            <td>IMAGE</td>
                            <td class="d-flex flex-row">
                                <a href="/annonces" class="btn btn-success rounded-pill ml-1">Voir</a>
                                <a href="" class="btn btn-primary rounded-pill ml-1">Éditer</a>
                                <a href="/panel/supprimeruneannonce/{{annonce.id}}" class="btn btn-danger rounded-pill ml-1">Supprimer</a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% endblock %}
    </div>

    {# Pied de page #}
    {% include 'footer.html.twig' %}
</body>
</html>