{# Entête de la page / Inclusion #}
{% include 'header.html.twig' %}
<body class="bg-light">

    {# Barre de Navigation #}
    {% include 'navigation.html.twig' %}

    {# Contenu de la page #}
    <div id="content" class="container mt-5 mb-5">
        <div class="container d-flex justify-content-between">
            <button type="button" class="btn btn-danger mb-2" id='previous-page-button'>Retour</button>
            <a href="/panel/annonces" class="btn btn-info mb-2">Voir les Annonces</a>
        </div>
        <div class="text-center mb-4">
            <h4>Modification d'une annonce</h4>
        </div>
        {% if success is defined %}
        <div class="p-3 mb-2 mt-2 text-center rounded bg-success text-white">
            <h3>{{success}}</h3>
        </div>
        {% endif %}
        {% block dashboard_create_announce %}
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary mb-2">Envoyer la demande</button>
            {{ form_end(form) }}
        {% endblock %}
    </div>

    {# Pied de page #}
    {% include 'footer.html.twig' %}
</body>
</html>