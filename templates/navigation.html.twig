
<nav class="navbar navbar-expand-lg navbar-light bg-dark nav">
    <a class="navbar-brand text-danger" href="{{ path('accueil') }}">
        <img src="{{ asset('assets/img/logo.png') }}" class="img-logo">
        Azera
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
            <li class="mr-2 nav-item">
                <a class="nav-link text-light" href="{{ path('accueil') }}">Accueil</a>
            </li>
            <li class="mr-2 nav-item">
                <a class="nav-link text-light" href="{{ path('annonces') }}">Annonces</a>
            </li>
            <li class="mr-2 nav-item">
                <a class="nav-link text-light" href="{{ path('contact') }}">Contact</a>
            </li>
            {% if app.user %}
            <li class="ml-3 mr-2 px-3 nav-item bg-primary rounded-pill">
                <div class="nav-link text-light" href="">{{ app.user.username }}</div>
            </li>
            <li class="mr-2 px-3 nav-item bg-danger rounded-pill">
                <a class="nav-link text-light" href="/deconnexion">Déconnexion</a>
            </li>
            {% else %}
            <li class="ml-2 mr-2 px-3 nav-item bg-primary rounded-pill">
                <a class="nav-link text-light" href="{{ path('connexion') }}">Connexion</a>
            </li>
            <li class="mr-2 px-3 nav-item bg-primary rounded-pill">
                <a class="nav-link text-light" href="{{ path('inscription') }}">S'inscrire</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
            <li class="mr-2 px-3 nav-item bg-warning rounded-pill">
                <a class="nav-link text-light" href="{{ path('panel') }}">Administration</a>
            </li>
            {% endif %}
        </ul>
    </div>
</nav>